import { Link, Navigate, Route, Routes } from 'react-router-dom'
import { LayoutDashboard, Users, CreditCard, Sparkles, Webhook, ScrollText } from 'lucide-react'
const items = [[LayoutDashboard,'Dashboard','/dashboard'],[Users,'Users','/users'],[CreditCard,'Payments','/payments'],[Sparkles,'Generations','/generations'],[Webhook,'Webhooks','/webhooks'],[ScrollText,'Audit logs','/audit-logs']] as const
function Layout({title}:{title:string}) { return <div className="min-h-screen bg-slate-950 text-slate-100 md:flex"><aside className="border-b border-slate-800 p-5 md:min-h-screen md:w-64 md:border-b-0 md:border-r"><Link to="/dashboard" className="text-lg font-bold">AI Day Admin</Link><nav className="mt-8 grid gap-2">{items.map(([Icon,label,to])=><Link key={to} to={to} className="flex items-center gap-3 rounded-lg px-3 py-2 text-sm text-slate-300 hover:bg-slate-900 hover:text-white"><Icon size={18}/>{label}</Link>)}</nav></aside><main className="flex-1 p-7"><p className="text-sm text-cyan-300">Operations</p><h1 className="mt-2 text-3xl font-bold">{title}</h1><div className="mt-8 rounded-2xl border border-slate-800 bg-slate-900 p-6 text-slate-300">Secure admin data will appear here after backend admin authorization is connected.</div></main></div> }
export default function App() { return <Routes><Route path="/login" element={<div className="grid min-h-screen place-items-center bg-slate-950 text-slate-100"><div className="w-full max-w-sm rounded-2xl border border-slate-800 bg-slate-900 p-7"><h1 className="text-2xl font-bold">Admin sign in</h1><p className="mt-2 text-sm text-slate-300">Use the configured administrator authentication method.</p><button className="mt-6 w-full rounded-xl bg-cyan-300 px-4 py-3 font-semibold text-slate-950">Continue</button></div></div>}/>{items.map(([,label,to])=><Route key={to} path={to} element={<Layout title={label}/>}/>)}<Route path="*" element={<Navigate to="/dashboard" replace/>}/></Routes> }
